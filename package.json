{
  "name": "breediq-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "comment": "Named 'compile', NOT 'build', on purpose: Vercel auto-runs a 'build' script and then expects a public/ output dir, which flips this static site into build-deploy mode and fails. 'compile' is local-only; the committed dashboard.bundle.js is what Vercel serves.",
    "compile": "node build.mjs",
    "compile:watch": "node build.mjs --watch"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.65.0",
    "@supabase/supabase-js": "^2.39.0",
    "nodemailer": "^6.9.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
